/**
 * BOM Configuration
 *
 * Centralized configuration for the 3-level BOM system:
 * - Product BOM Template (structure + defaults)
 * - Variation BOM Override (fabric colors + overrides)
 * - SKU BOM Line (size-specific overrides)
 *
 * Includes component types, roles, and categories.
 */

export * from './componentTypes.js';
