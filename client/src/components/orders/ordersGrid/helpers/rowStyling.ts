/**
 * Row styling utilities for OrdersGrid
 *
 * This file re-exports from the centralized formatting system.
 * For new code, import directly from '../formatting' instead.
 *
 * @deprecated Import from '../formatting' for new code
 */

export {
    getRowStyle,
    getRowClass,
    gridRowStyles,
} from '../formatting';
